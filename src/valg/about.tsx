import React from 'react'
import ReactDOM from 'react-dom/client'

/* i stedet for root, mer beskrivende navn */
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <h1>En annen valg-app!</h1>
  </React.StrictMode>,
)
